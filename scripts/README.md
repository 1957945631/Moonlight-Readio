# Scripts

This folder is reserved for project utility scripts.

Current common commands live in `package.json`:

```powershell
npm start
npm test
npm run ncm -- login --check
```

Add scripts here only when a repeatable task cannot be expressed cleanly as an npm script.
